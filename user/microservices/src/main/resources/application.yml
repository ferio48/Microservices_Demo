server:
  port: 8081

spring:
  application:
    name: user-service

eureka:
  instance:
    hostname: localhost

management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus